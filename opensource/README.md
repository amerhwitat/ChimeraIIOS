# Chimera II open-source compatibility binding

This language directory exposes the same service/application metadata model defined by `opensource/`, `services/` and `applications/`. It is an adapter API, not a rewritten copy of upstream projects. Upstream source and license provenance is preserved in the canonical registries.
