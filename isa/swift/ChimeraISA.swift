public enum ChimeraISA {
    public static let catalogVersion = "CHM-ISA-CATALOG-1"
    public static let catalogPath = "isa/catalog.json"
    public struct InstructionRef { public let id, family, mnemonic, syntax, hex: String }
}
