# Build status

The repository changes in this tranche were written through the GitHub repository API. Local compilation was not performed in this environment, so no local build-pass claim is made.

The committed CI workflow is intended to perform the portable building-block compilation and CTest run on GitHub-hosted runners. A successful workflow run must be observed before declaring the CI build green.
