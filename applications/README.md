# Application compatibility tree

This tree is a catalog and adapter boundary, not a copy of entire Linux/Windows/macOS application ecosystems. Use the provenance registry to acquire a permitted upstream source, then build it through an explicit, sandboxed toolchain policy.
